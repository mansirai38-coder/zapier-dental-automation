# Screenshots

This folder is where real screenshots of the live Zapier dashboard belong. Suggested files (already referenced from the main README):

- `zaps-list.png` — the Zaps dashboard showing all 6 published automations
- `zap-detail-example.png` — one Zap opened, showing its steps (e.g. "New Appointment Confirmation & Calendar")

Add these with exactly these filenames and the links in the main README will render automatically.
